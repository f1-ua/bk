import subprocess
import sys
import os
import argparse

def show_intro():
    intro = """
================================================================
          FFmpeg 智能媒体转换工具 (Python 版)
================================================================
原理说明：
1. H.264 (AVC): 兼容性之王。采用帧间预测和变换编码，几乎所有硬件都支持。
2. H.265 (HEVC): 空间节省者。通过更复杂的计算（CTU 结构），同画质下体积比 H.264 小 50%。
3. AV1: 极致压缩。开源无版税格式，拥有目前最高的压缩效率，适合长期归档。
4. CRF 机制: '恒定质量'模式。它会根据画面复杂度自动分配码率，实现画质与体积的动态平衡。
5. 封装 (Copy): 仅改变文件后缀（容器），不重新编码，实现 0 画质损失和秒级转换。
================================================================
    """
    print(intro)

def run_ffmpeg(cmd):
    try:
        subprocess.run(cmd, check=True)
        print("\n[成功] 处理完成！")
    except subprocess.CalledProcessError:
        print("\n[错误] FFmpeg 执行失败，请检查是否安装了 FFmpeg 或输入文件是否有误。")
    except FileNotFoundError:
        print("\n[错误] 未找到 FFmpeg，请确保它已添加到系统环境变量。")

def main():
    parser = argparse.ArgumentParser(description="FFmpeg 媒体转换工具")
    parser.add_argument("input", help="输入文件路径 (如: video.webm)")
    parser.add_argument("-m", "--mode", type=int, choices=[1, 2, 3, 4, 5], 
                        help="转换模式: 1-兼容(H.264), 2-省空间(H.265), 3-极致(AV1), 4-提音频(MP3), 5-快封(Copy)")
    
    # 如果没有参数，显示帮助信息
    if len(sys.argv) == 1:
        show_intro()
        parser.print_help()
        sys.exit(0)

    args = parser.parse_args()
    input_file = args.input
    base_name = os.path.splitext(input_file)[0]

    # 定义转换逻辑
    # 模式字典: {模式号: [视频编码, 预设, CRF, 音频编码, 后缀]}
    modes = {
        1: ["libx264", "slow", "22", "aac", "mp4", "兼容模式 (H.264)"],
        2: ["libx265", "slow", "24", "aac", "mp4", "省空间模式 (H.265)"],
        3: ["libsvtav1", "6", "30", "libopus", "mkv", "极致压缩模式 (AV1)"],
    }

    if args.mode in [1, 2, 3]:
        v_codec, preset, crf, a_codec, ext, desc = modes[args.mode]
        print(f"正在执行: {desc}")
        cmd = [
            "ffmpeg", "-i", input_file,
            "-c:v", v_codec, "-preset", preset, "-crf", crf,
            "-c:a", a_codec, "-b:a", "128k",
            f"{base_name}_out.{ext}"
        ]
        run_ffmpeg(cmd)

    elif args.mode == 4:
        print("正在提取高质量音频 (MP3)...")
        cmd = ["ffmpeg", "-i", input_file, "-vn", "-c:a", "libmp3lame", "-q:a", "2", f"{base_name}.mp3"]
        run_ffmpeg(cmd)

    elif args.mode == 5:
        print("正在进行快速封装转换 (No Re-encoding)...")
        cmd = ["ffmpeg", "-i", input_file, "-c", "copy", f"{base_name}_copy.mp4"]
        run_ffmpeg(cmd)

if __name__ == "__main__":
    main()